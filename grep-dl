#!/usr/bin/perl

$cmd = "";
for (@ARGV) {
    if (!length($cmd)) { $cmd = "grep -i '$_' ~/notes/2dl ~/notes/todl.txt ~/notes/donedl ~/notes/done-deleted-dl.txt" }
    else { $cmd .= " | grep -i '$_'"; }
}

print "# $cmd\n";
system $cmd;

