#!/usr/bin/perl -pi
#000605. i've perhaps recreated this script ten times or so.
#000817. inplace version.
$. = 1 if $ARGV ne $old_argv;
print /\S/ ? sprintf("%4d|", $.) : "    |";
$old_argv = $ARGV;
